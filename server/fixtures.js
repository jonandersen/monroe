if (Destinations.find().count() === 0) {
    Destinations.insert({
        name: 'Norway'
    });
    Destinations.insert({
        name: 'Sweden'
    });
    Destinations.insert({
        name: 'Denmark'
    });
}